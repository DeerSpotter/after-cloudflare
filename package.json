{
  "name": "after-cloudflare",
  "version": "0.1.0",
  "description": "Open source edge runtime and router for programmable request handling, multi CDN failover, provider neutral traffic control, and resilient internet delivery.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "local": "node scripts/local-edge.mjs",
    "simulate": "node scripts/simulate-network.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
